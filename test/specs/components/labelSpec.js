/**
 * Created by dmitriy.ryajov on 7/8/14.
 */
define(
    ['framework/components/label'],
    function (label) {
        'use strict';

        describe('label component specs', function(){
            it('label should exist', function(){
                expect(label).not.toBe(null);
            })
        });
    });
